{
  "name": "open-ipo-listing-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic pre-listing readiness service for the proposed Orbital Exchange.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
