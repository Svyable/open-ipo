{
  "name": "open-ipo",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub-native standards, validation, and readiness tooling for the proposed Orbital Exchange.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
